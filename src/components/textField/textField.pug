include ../label/label

mixin textField(options = {})
	.textField
		+label(options.label, options.additionalLabel)
			input(type=options.type, placeholder=options.placeholder, class=options.classes, value=options.value)